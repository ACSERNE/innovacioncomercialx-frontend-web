# placeholder Dockerfile for frontend-mobile
FROM node:20-alpine
WORKDIR /app
# evita instalar nada; solo permite que docker-compose build funcione
RUN echo "placeholder frontend-mobile" > /placeholder.txt
CMD ["sh", "-c", "cat /placeholder.txt && sleep infinity"]
