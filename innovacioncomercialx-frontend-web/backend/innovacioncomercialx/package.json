{
  "name": "innovacioncomercialx",
  "version": "1.0.0",
  "scripts": {
    "check-env": "node scripts/checkEnv.js",
    "fix-app": "node scripts/fixApp.js",
    "update-deps": "node scripts/updateDeps.js",
    "docker-up": "docker compose down -v && docker compose build --no-cache && docker compose up -d",
    "wait-db": "node scripts/waitPostgres.js",
    "logs": "docker compose logs -f backend frontend-web frontend-mobile",
    "levantar": "npm run check-env && npm run fix-app && npm run update-deps && npm run docker-up && npm run wait-db && npm run logs"
  }
}
